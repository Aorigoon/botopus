name: botopus
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  xterm: ^3.5.0
  flutter_pty: ^0.4.2
  path_provider: ^2.1.1
  http: ^1.1.0
  archive: ^3.3.7

flutter:
  uses-material-design: true
  assets:
    - assets/proot
    - assets/alpine-rootfs.tar.gz
