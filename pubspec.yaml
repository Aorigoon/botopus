name: botopus
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  url_launcher: ^6.2.1
  flutter:
    sdk: flutter
  xterm: ^4.0.0
  flutter_pty: ^0.4.2
  path_provider: ^2.1.1
  http: ^1.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/proot
    - assets/ubuntu-rootfs.tar.gz
    - assets/libtalloc.so.2
    - assets/libandroid-shmem.so
    - assets/loader
    - assets/loader32
